export default function RootLoading() {
    return (
        <div className="page-wash min-h-screen flex items-center justify-center">
            <div className="w-10 h-10 border-2 border-[var(--line)] border-t-[var(--brand-green)] rounded-full animate-spin" />
        </div>
    );
}
